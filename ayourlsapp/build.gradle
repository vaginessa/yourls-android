apply plugin: 'com.android.application'

def extraGradle = file('mateware.gradle')
if (extraGradle.exists())
    apply from: extraGradle

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.1"

//    Properties localProperties = new Properties();
//    localProperties.load(project.rootProject.file('local.properties').newDataInputStream())
//
//    if(!localProperties.getProperty('matestuff')) throw new Exception ("matestuff must be set")
//    def mateStuff = new File(localProperties.getProperty('matestuff'))
//
//
//    Properties signingProperties = new Properties();
//    signingProperties.load(new File(mateStuff,name+'.signing').newDataInputStream())
//
//    signingConfigs {
//        debug {
//            storeFile new File(mateStuff,signingProperties.getProperty('debugStoreFile'))
//        }
//        release {
//            storeFile new File(mateStuff,signingProperties.getProperty('storeFile'))
//            storePassword signingProperties.getProperty('storePassword')
//            keyAlias signingProperties.getProperty('keyAlias')
//            keyPassword signingProperties.getProperty('keyPassword')
//        }
//    }

    defaultConfig {
        applicationId "de.mateware.ayourls"
        minSdkVersion 7
        targetSdkVersion 23
        versionCode 1
        versionName "1.0" + versionCode
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    lintOptions {
        abortOnError false
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:23.0.1'
    compile 'com.android.support:support-v4:23.0.1'
    compile 'com.android.support:preference-v7:23.0.1'
    compile 'com.android.support:recyclerview-v7:23.0.1'
    compile 'com.github.tony19:logback-android-core:1.1.1-4'
    compile 'com.github.tony19:logback-android-classic:1.1.1-4'
    compile 'org.slf4j:slf4j-api:1.7.12'
    compile 'com.mcxiaoke.volley:library:1.0.19'
    compile('commons-validator:commons-validator:1.4.1') {
        exclude group: 'commons-beanutils', module: 'commons-beanutils'
    }
}


